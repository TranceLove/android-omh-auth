# Module core-common-mobileweb

This module contains common classes that can facilitate (easier) implementation of OAuth provider integrations using AndroidX Browser CustomTab.

This allows integrating OAuth providers that doesn't provide SDK, or SDK integration method doesn't fit the required usage scenario.

These classes are extracted from the original plugin-google-non-gms module.
